import { initNav, initMobileMenu, initScrollProgress, setActiveNavLink, initBookingForm } from './shared.js';

initNav();
initMobileMenu();
initScrollProgress();
setActiveNavLink();
initBookingForm();
